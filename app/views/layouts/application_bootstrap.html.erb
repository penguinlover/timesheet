<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Timesheet Beta" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <link href='http://fonts.googleapis.com/css?family=Marck+Script' rel='stylesheet' type='text/css'>
    <%= javascript_include_tag "application" %>
  </head>

  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="#" style="font-size:28px;">Timesheet</a>
          <div class="container nav-collapse">
            <ul class="nav" style="float:right;">
              <li><%= link_to "Help", "/help"  %></li>
              <% if current_user %>
              <li><%= link_to "Log out", log_out_path %></li>
              <% else %>
              <li><%= link_to "Log in", root_path %></li>
              <% end %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row">
          <% flash.each do |name, msg| %>
            <%= content_tag :div, :id => "flash-#{name}", :class => "alert alert-#{name == :notice ? 'success' : 'error'}" do %>
              <a class="close" href="#" data-dismiss="alert">x</a>
              <%= msg %>
            <% end %>
          <% end %>
        </div>
        <div class="row">
          <%= yield %>
        </div>
      </div><!--/content-->

      <footer style="text-align:right;margin-top:60px;">
        <p>&copy; ComityDesigns Inc. 2012</p>
      </footer>
    </div> <!-- /container -->

    <script>
      (function() { 
      var fLastCount = -1;
      window.setInterval(function()
      {
        try
        {
         var res = document.title.match(/^\((\d+)\)/);
         if(res) {
            var nNewCount = res[1];
            if(nNewCount != fLastCount) {
              var ico = nNewCount > 5 ? 'error' : 'num_'+nNewCount;
              window.external.msSiteModeSetIconOverlay('http://ie.microsoft.com/testdrive/Browser/tweetfeed/Images/'+ico+'.ico', nNewCount+' unread tweets');
              fLastCount = nNewCount;
            }
          }
          else if(fLastCount != 0) {
            window.external.msSiteModeClearIconOverlay();
            fLastCount = 0;
          }
        } catch(e) { }
      }, 1000);
      })();
    </script>

  </body>
</html>
