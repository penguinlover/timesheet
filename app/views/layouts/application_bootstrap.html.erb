<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Timesheet Beta" %></title>
    <%= csrf_meta_tags %>
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->
    <%= stylesheet_link_tag "application", :media => "all" %>
    <link href='http://fonts.googleapis.com/css?family=Marck+Script' rel='stylesheet' type='text/css'>
  </head>

  <body>
    <header id="main_header" class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href="#" style="font-size:28px;">Timesheet</a>
            <ul class="nav" style="float:right;">
              <li id="user_bio">
                <span><%= gravatar_for current_user %></span>
                <span><%= current_user.email %></span>
              </li>
              <% if current_user %>
              <li><%= link_to "Log out", log_out_path %></li>
              <% else %>
              <li><%= link_to "Log in", root_path %></li>
              <% end %>
              <li><%= link_to "Help", "#"  %></li>
            </ul>
        </div>
      </div>
    </header>

    <%= yield %>

    <footer id="main_footer">
      <p>Qiang Han  &copy; ComityDesigns Inc. 2013</p>
    </footer>

    <%= javascript_include_tag "application" %>
    <%= yield :javascript %>
  </body>
</html>
